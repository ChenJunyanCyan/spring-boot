# 轨道堆叠合成游戏设计方案

## 一、游戏概述

**游戏类型**：2D 休闲益智类微信小游戏 / 原生 App

**核心玩法**：玩家控制发射台，将球体沿抛物线轨道落入固定棍子，当同棍子上累积 3 个及以上相同等级球体时触发合成，生成更高级球体。

**视觉风格**：简约卡通风，鲜艳配色，圆润造型

---

## 二、页面结构

```
┌─────────────────────────────────────────┐
│  [Next球预览]           [当前分数/关卡] │  ← 顶部 UI 栏
├─────────────────────────────────────────┤
│                                         │
│         ┌─────────────────┐             │
│         │   目标球区域     │             │  ← 顶部目标区
│         └─────────────────┘             │
│                                         │
│    ┃        ┃        ┃        ┃        ┃   ← 5 根固定棍子
│    ┃   ●2   ┃        ┃   ●3   ┃        ┃
│    ┃   ●2   ┃   ●1   ┃   ●3   ┃   ●4   ┃   ← 球堆叠区域
│    ┃   ●1   ┃   ●1   ┃        ┃        ┃
│  ═══════════════════════════════════  │  ← 地面
├─────────────────────────────────────────┤
│      [←滑动发射方向与力度→]              │  ← 底部提示/触摸区
└─────────────────────────────────────────┘
```

---

## 三、核心数据结构

### 3.1 球体数据结构

```typescript
interface Ball {
  id: number;           // 唯一标识
  level: number;        // 等级 (1, 2, 3, ... 数值越大越高)
  color: string;       // 颜色 (如 "#FF6B6B")
  x: number;           // 当前 X 坐标
  y: number;           // 当前 Y 坐标
  stickId: number;     // 所在棍子 ID (-1 表示在空中)
  state: 'flying' | 'resting' | 'dissolving';
}
```

### 3.2 棍子数据结构

```typescript
interface Stick {
  id: number;
  x: number;           // 棍子中心 X 坐标
  y: number;           // 棍子底部 Y 坐标 (固定不变)
  balls: Ball[];       // 该棍子上的球体列表 (从下到上排序)
  maxHeight: number;   // 最大堆叠高度
}
```

### 3.3 游戏状态

```typescript
interface GameState {
  sticks: Stick[];         // 5 根棍子
  currentBall: Ball;       // 当前待发射球
  nextBall: Ball;          // 预览的下一个球
  score: number;
  gamePhase: 'ready' | 'aiming' | 'flying' | 'landing' | 'merging' | 'gameover';
}
```

---

## 四、贝塞尔曲线发射系统

### 4.1 发射原理

球体沿**三阶贝塞尔曲线**运动，发射角度和力度共同决定曲线路径。

```
发射点 (Start)
    ●
     \
      \  ← 控制点1 (Control1)
       \
        ······← 控制点2 (Control2)
              \
               ● 落点 (End)
```

### 4.2 贝塞尔曲线公式

**三阶贝塞尔曲线参数方程**：
```
B(t) = (1-t)³·P0 + 3(1-t)²·t·P1 + 3(1-t)·t²·P2 + t³·P3

其中：
  t ∈ [0, 1]  — 时间参数
  P0         — 起始点 (发射台位置)
  P1         — 控制点1 (发射方向延伸)
  P2         — 控制点2 (落点方向延伸)
  P3         — 终止点 (目标棍子位置)
```

### 4.3 代码实现

```typescript
// 贝塞尔曲线计算
function bezierCurve(t: number, p0: Point, p1: Point, p2: Point, p3: Point): Point {
  const mt = 1 - t;
  const mt2 = mt * mt;
  const mt3 = mt2 * mt;
  const t2 = t * t;
  const t3 = t2 * t;

  return {
    x: mt3 * p0.x + 3 * mt2 * t * p1.x + 3 * mt * t2 * p2.x + t3 * p3.x,
    y: mt3 * p0.y + 3 * mt2 * t * p1.y + 3 * mt * t2 * p2.y + t3 * p3.y
  };
}

// 计算控制点
function calculateControlPoints(start: Point, end: Point, angle: number, power: number): Point[] {
  const dx = end.x - start.x;
  const dy = end.y - start.y;
  const distance = Math.sqrt(dx * dx + dy * dy);

  // 控制点1: 沿发射方向延伸
  const control1 = {
    x: start.x + Math.cos(angle) * power * 0.5,
    y: start.y + Math.sin(angle) * power * 0.5
  };

  // 控制点2: 落点方向延伸
  const control2 = {
    x: end.x - Math.cos(angle) * power * 0.3,
    y: end.y - Math.sin(angle) * power * 0.3
  };

  return [start, control1, control2, end];
}

// 发射球体
function launchBall(startPos: Point, targetStick: Stick, angle: number, power: number): Ball {
  const endPos = { x: targetStick.x, y: targetStick.y };

  const [, p1, p2, p3] = calculateControlPoints(startPos, endPos, angle, power);

  let t = 0;
  const speed = 0.05; // 每帧推进 5%

  function updatePosition() {
    t += speed;
    if (t >= 1) {
      ball.x = targetStick.x;
      ball.y = targetStick.y;
      return 'arrived';
    }

    const pos = bezierCurve(t, startPos, p1, p2, p3);
    ball.x = pos.x;
    ball.y = pos.y;

    return 'flying';
  }

  return ball;
}
```

### 4.4 玩家输入控制

```typescript
// 触摸滑动控制发射
function onTouchMove(touchStartX: number, touchStartY: number, touchEndX: number, touchEndY: number) {
  const dx = touchEndX - touchStartX;
  const dy = touchEndY - touchStartY;

  // 发射角度 (与下落方向相反)
  const angle = Math.atan2(-dy, dx);

  // 发射力度 (根据滑动距离)
  const distance = Math.sqrt(dx * dx + dy * dy);
  const power = Math.min(distance / 100, 2); // 力度范围 [0, 2]

  // 显示预测轨迹
  showTrajectoryPreview(startPos, targetStick.x, angle, power);
}
```

---

## 五、物理吸附系统

### 5.1 吸附触发条件

当球体接近棍子顶部时，进行吸附判定：

```typescript
interface AdsorptionConfig {
  maxDistanceX: number;    // X 方向容差 (默认 30px)
  maxDistanceY: number;   // Y 方向容差 (默认 20px)
  adsorptionSpeed: number; // 吸附动画速度
}
```

### 5.2 吸附逻辑实现

```typescript
const ADSORPTION_CONFIG = {
  maxDistanceX: 30,
  maxDistanceY: 20,
  adsorptionSpeed: 0.15
};

function checkAdsorption(ball: Ball, sticks: Stick[]): Stick | null {
  for (const stick of sticks) {
    // 计算球体与棍子的距离
    const dx = Math.abs(ball.x - stick.x);
    const dy = ball.y - stick.y; // 球在棍子上方

    // 判断是否在吸附范围内
    if (dx <= ADSORPTION_CONFIG.maxDistanceX && dy >= 0 && dy <= ADSORPTION_CONFIG.maxDistanceY) {
      return stick;
    }
  }
  return null;
}

function adsorbBallToStick(ball: Ball, stick: Stick): void {
  // 锁定 X 坐标到棍子中心
  const targetX = stick.x;

  // 动画吸附
  function adsorptionFrame() {
    const dx = targetX - ball.x;
    const dy = ball.y - (stick.y + getStackHeight(stick));

    ball.x += dx * ADSORPTION_CONFIG.adsorptionSpeed;
    ball.y += dy * ADSORPTION_CONFIG.adsorptionSpeed;

    if (Math.abs(dx) < 0.5 && Math.abs(dy) < 0.5) {
      ball.x = targetX;
      ball.y = stick.y + getStackHeight(stick);
      placeBallOnStick(ball, stick);
      return;
    }

    requestAnimationFrame(adsorptionFrame);
  }

  adsorptionFrame();
}
```

### 5.3 堆叠高度计算

```typescript
function getStackHeight(stick: Stick): number {
  if (stick.balls.length === 0) return 0;

  const bottomBall = stick.balls[0];
  const topBall = stick.balls[stick.balls.length - 1];

  return topBall.y - bottomBall.y + BALL_RADIUS * 2;
}

function placeBallOnStick(ball: Ball, stick: Stick): void {
  // 找到正确的插入位置 (按 Y 从下到上)
  let insertIndex = 0;
  for (let i = 0; i < stick.balls.length; i++) {
    if (ball.y > stick.balls[i].y) {
      insertIndex = i + 1;
    }
  }

  stick.balls.splice(insertIndex, 0, ball);
  ball.stickId = stick.id;

  // 更新该棍子所有球的层级
  updateBallZIndex(stick);

  // 检查是否触发合成
  checkMerge(stick, insertIndex);
}
```

---

## 六、合成逻辑系统

### 6.1 合成规则

| 条件 | 结果 |
|------|------|
| 同棍子上 ≥ 3 个连续相同等级球 | 触发合成 |
| 合成后生成 1 个更高级球 | 放在原来 3 个球的中位置 |
| 合成后剩余球体下落 | 重新排列堆叠 |

### 6.2 合成判定流程

```
┌─────────────────────────────────┐
│  球体落定，插入棍子列表          │
└──────────────┬──────────────────┘
               ▼
┌─────────────────────────────────┐
│  从插入位置向上下扩展查找        │
│  连续相同等级的球                │
└──────────────┬──────────────────┘
               ▼
┌─────────────────────────────────┐
│  找到连续相同等级球 ≥ 3 个？     │
└──────────────┬──────────────────┘
         YES   │   NO
    ┌──────────┴──────────┐
    ▼                     ▼
┌─────────────┐    ┌─────────────┐
│  触发合成   │    │  结束       │
│  播放动画   │    │  检测下一个球│
└─────────────┘    └─────────────┘
```

### 6.3 合成代码实现

```typescript
interface MergeResult {
  merged: boolean;
  newBall: Ball | null;
  dissolvedBalls: Ball[];
  newStack: Ball[];
}

function checkMerge(stick: Stick, insertedIndex: number): MergeResult {
  const ball = stick.balls[insertedIndex];
  const level = ball.level;

  // 向上查找连续相同等级
  let upCount = 0;
  for (let i = insertedIndex; i < stick.balls.length; i++) {
    if (stick.balls[i].level === level) {
      upCount++;
    } else {
      break;
    }
  }

  // 向下查找连续相同等级
  let downCount = 0;
  for (let i = insertedIndex - 1; i >= 0; i--) {
    if (stick.balls[i].level === level) {
      downCount++;
    } else {
      break;
    }
  }

  const totalCount = upCount + downCount - 1; // 减去插入球本身

  if (totalCount >= 3) {
    return executeMerge(stick, insertedIndex, level, upCount, downCount);
  }

  return { merged: false, newBall: null, dissolvedBalls: [], newStack: stick.balls };
}

function executeMerge(stick: Stick, insertedIndex: number, level: number, upCount: number, downCount: number): MergeResult {
  // 找到所有需要合并的球的索引
  const mergeStart = insertedIndex - downCount + 1;
  const mergeEnd = insertedIndex + upCount - 1;
  const mergeCount = mergeEnd - mergeStart + 1;

  // 获取要溶解的球
  const dissolvedBalls = stick.balls.slice(mergeStart, mergeEnd + 1);

  // 计算新球位置 (中间位置)
  const middleIndex = Math.floor(mergeCount / 2);
  const newBallY = dissolvedBalls[middleIndex].y;

  // 标记溶解动画
  dissolvedBalls.forEach(ball => {
    ball.state = 'dissolving';
    playDissolveAnimation(ball);
  });

  // 创建新球 (等级 +1)
  const newBall = createBall(level + 1);
  newBall.x = stick.x;
  newBall.y = newBallY;
  newBall.state = 'resting';

  // 延迟移除旧球，创建新球
  setTimeout(() => {
    // 移除旧球
    stick.balls = stick.balls.filter(b => b.state !== 'dissolving');

    // 插入新球到正确位置
    let insertPos = 0;
    for (let i = 0; i < stick.balls.length; i++) {
      if (newBall.y > stick.balls[i].y) {
        insertPos = i + 1;
      }
    }
    stick.balls.splice(insertPos, 0, newBall);

    // 重新排列并更新层级
    rearrangeStack(stick);

    // 检查是否有新的合成链
    checkMergeChain(stick);
  }, DISSOLVE_ANIMATION_DURATION);

  return {
    merged: true,
    newBall,
    dissolvedBalls,
    newStack: stick.balls
  };
}
```

### 6.4 合成链检测 (级联合成)

```typescript
function checkMergeChain(stick: Stick): void {
  // 递归检查是否有新的合成触发
  for (let i = 0; i < stick.balls.length - 2; i++) {
    const result = checkMerge(stick, i);
    if (result.merged) {
      // 等待合成动画完成后继续检查
      setTimeout(() => checkMergeChain(stick), DISSOLVE_ANIMATION_DURATION);
      return;
    }
  }
}
```

---

## 七、视觉层级管理

### 7.1 球体渲染顺序

等级越高的球，渲染层级 (Z-Index) 越大，确保覆盖下面的低级球。

```typescript
function updateBallZIndex(stick: Stick): void {
  // 按 Y 坐标从下到上排序
  const sortedBalls = [...stick.balls].sort((a, b) => a.y - b.y);

  sortedBalls.forEach((ball, index) => {
    // 等级越高，层级越大
    ball.zIndex = index * 10 + ball.level;
  });
}

function renderBalls(ctx: CanvasRenderingContext2D, balls: Ball[]): void {
  // 按 zIndex 从小到大排序绘制
  const sortedBalls = balls.sort((a, b) => a.zIndex - b.zIndex);

  sortedBalls.forEach(ball => {
    drawBall(ctx, ball);
    drawBallNumber(ctx, ball);
  });
}
```

### 7.2 球体绘制

```typescript
const BALL_RADIUS = 25;
const BALL_COLORS: Record<number, string> = {
  1: '#FF6B6B',  // 红色
  2: '#4ECDC4',  // 青色
  3: '#45B7D1',  // 蓝色
  4: '#96CEB4',  // 绿色
  5: '#FFEAA7',  // 黄色
  6: '#DDA0DD',  // 紫色
  // ... 更多等级颜色
};

function drawBall(ctx: CanvasRenderingContext2D, ball: Ball): void {
  const color = BALL_COLORS[ball.level] || '#CCCCCC';

  // 绘制球体
  ctx.beginPath();
  ctx.arc(ball.x, ball.y, BALL_RADIUS, 0, Math.PI * 2);
  ctx.fillStyle = color;
  ctx.fill();

  // 绘制边框高光
  ctx.strokeStyle = 'rgba(255, 255, 255, 0.3)';
  ctx.lineWidth = 3;
  ctx.stroke();
}

function drawBallNumber(ctx: CanvasRenderingContext2D, ball: Ball): void {
  ctx.font = `bold ${BALL_RADIUS}px Arial`;
  ctx.fillStyle = '#FFFFFF';
  ctx.textAlign = 'center';
  ctx.textBaseline = 'middle';
  ctx.fillText(ball.level.toString(), ball.x, ball.y);
}
```

---

## 八、UI 交互设计

### 8.1 发射控制区域

```
        触摸区域 (底部 40% 屏幕)
        ┌─────────────────────────────────┐
        │                                 │
        │    ← 滑动方向决定发射角度        │
        │         ↓                       │
        │      滑动距离决定力度            │
        │                                 │
        └─────────────────────────────────┘
```

### 8.2 轨迹预览

```typescript
function showTrajectoryPreview(startPos: Point, targetX: number, angle: number, power: number): void {
  const endPos = { x: targetX, y: GROUND_Y };
  const [, p1, p2, p3] = calculateControlPoints(startPos, endPos, angle, power);

  // 绘制预测点
  for (let t = 0; t <= 1; t += 0.1) {
    const pos = bezierCurve(t, startPos, p1, p2, p3);
    drawDot(ctx, pos.x, pos.y, 'rgba(255, 255, 255, 0.5)');
  }
}
```

### 8.3 UI 组件布局

```
┌─────────────────────────────────────────┐
│ [下一个球]              [分数: 12500]   │
│  ┌─────┐                  [关卡: 3]     │
│  │  3  │                               │
│  └─────┘                               │
├─────────────────────────────────────────┤
│                                         │
│              (游戏区域)                  │
│                                         │
└─────────────────────────────────────────┘
```

---

## 九、状态机流程

```
                    ┌─────────────┐
                    │   READY     │ ← 初始状态，等待玩家输入
                    └──────┬──────┘
                           │ onTouchStart
                           ▼
                    ┌─────────────┐
         ┌─────────│   AIMING    │ ← 玩家滑动，实时预览轨迹
         │         └──────┬──────┘
         │                │ onTouchEnd
         │                ▼
         │         ┌─────────────┐
         │         │   FLYING    │ ← 球体沿贝塞尔曲线飞行
         │         └──────┬──────┘
         │                │ ball.arrived
         │                ▼
         │         ┌─────────────┐
         │    ┌───│  LANDING    │ ← 球体吸附到棍子
         │    │   └──────┬──────┘
         │    │          │ afterAdsorption
         │    │          ▼
         │    │   ┌─────────────┐
         │    └───│  MERGING    │ ← 检测并执行合成
         │        └──────┬──────┘
         │               │ afterMerge
         │               ▼
         │        ┌─────────────┐
         │        │   READY     │ ← 回到初始状态，发射下一个球
         │        └─────────────┘
         │
         │   ┌─────────────┐
         └───│  GAME_OVER  │ ← 棍子堆满或达到目标
              └─────────────┘
```

---

## 十、性能优化建议

1. **对象池**：预创建球体对象，避免运行时频繁 GC
2. **脏矩形渲染**：只重绘变化区域
3. **吸附检测节流**：每帧最多检测一次
4. **合成锁定**：合成动画期间锁定该棍子，防止重复触发

---

## 十一、技术选型建议

| 平台 | 推荐引擎 | 理由 |
|------|---------|------|
| 微信小游戏 | Cocos Creator | 2D 物理和 UI 支持极佳 |
| Web 网页版 | Phaser.js | 轻量级，成熟开源 |
| 原生 App | Unity 2D | 跨平台，生态完善 |

---

## 附录：核心文件结构

```
src/
├── game/
│   ├── GameManager.ts      // 游戏主控
│   ├── Ball.ts            // 球体类
│   ├── Stick.ts           // 棍子类
│   ├── Launcher.ts        // 发射系统
│   ├── Adsorption.ts      // 物理吸附
│   ├── Merger.ts          // 合成逻辑
│   └── UIManager.ts       // UI 管理
├── utils/
│   ├── Bezier.ts          // 贝塞尔曲线工具
│   └── Constants.ts       // 常量定义
└── main.ts                // 入口文件
```